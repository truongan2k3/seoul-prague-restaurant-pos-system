import { DashboardShell } from "@/components/dashboard-shell";

export const metadata = {
  title: "POS — SEOUL PRAGUE",
  description: "Restaurant point of sale",
};

export default function PosAppPage() {
  return <DashboardShell />;
}
